import { DNS } from "./discovery/dns";
import { LocationToGeoDomain } from "./discovery/location-to-geo-domain";
import { LocationToServerAddr } from "./discovery/location-to-servers";

export { DNS, LocationToGeoDomain, LocationToServerAddr };
