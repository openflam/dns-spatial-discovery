import { DNS } from "./dns";
import { LocationToGeoDomain } from "./location-to-geo-domain";
import { LocationToServerAddr } from "./location-to-servers";

export { DNS, LocationToGeoDomain, LocationToServerAddr };
